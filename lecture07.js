/**
 * @title : Binary Seach
 *
 *
 */

/**
 * Loop Start
 * mid = (first_index+last_index)/2
 *
 * if array[mid] is equal to key;
 * return mid
 *
 *
 * if array[mid] is greater than key:
 * last_index = mid-1
 *
 *
 * if array[mid] is smaller than key:
 * first_index = mid+1;
 *
 *
 * Loop End:
 */
